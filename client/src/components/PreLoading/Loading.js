import "./Loading.css";

const Loading = ({ a }) => {
  return (
    <div className="example">
      <span className="smooth spinner">
        <h1>Please Wait...</h1>
      </span>
    </div>
  );
};

export default Loading;
